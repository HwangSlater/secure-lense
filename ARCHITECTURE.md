# SecureLens 시스템 아키텍처

이 문서는 SecureLens의 전체 시스템 아키텍처를 설명합니다.

---

## 📐 전체 아키텍처 개요

SecureLens는 **마이크로서비스 아키텍처**를 기반으로 하며, Docker 컨테이너로 구성된 분산 시스템입니다.

```
┌─────────────────────────────────────────────────────────────────┐
│                         사용자 (브라우저)                        │
└────────────────────────────┬────────────────────────────────────┘
                             │ HTTPS
                             ▼
                    ┌────────────────┐
                    │   Nginx (80)   │  ← 리버스 프록시
                    │  Reverse Proxy │
                    └────────┬───────┘
                             │
                ┌────────────┴────────────┐
                │                         │
                ▼                         ▼
        ┌───────────────┐        ┌───────────────┐
        │   Frontend    │        │    Backend    │
        │  Next.js:3000 │        │ FastAPI:8000  │
        │   (React)     │        │   (Python)    │
        └───────────────┘        └───────┬───────┘
                                         │
                    ┌────────────────────┼────────────────────┐
                    │                    │                    │
                    ▼                    ▼                    ▼
            ┌──────────────┐    ┌──────────────┐    ┌──────────────┐
            │    MySQL     │    │   ClamAV     │    │ External APIs│
            │   :3306      │    │   :3310      │    │              │
            │  (Database)  │    │ (Antivirus)  │    │ - VirusTotal │
            └──────────────┘    └──────────────┘    │ - MalwareBzr │
                                                    │ - URLScan.io │
                                                    │ - IP-API     │
                                                    │ - Gemini AI  │
                                                    └──────────────┘
```

---

## 🏗️ 계층별 아키텍처

### 1. 프레젠테이션 계층 (Presentation Layer)

**구성 요소**: Frontend (Next.js)

```
┌─────────────────────────────────────────┐
│         Frontend (Next.js)              │
├─────────────────────────────────────────┤
│  • React Components                     │
│  • TypeScript                           │
│  • Tailwind CSS                         │
│  • Client-side Routing                  │
└─────────────────────────────────────────┘
```

**주요 컴포넌트**:
- `FileUpload.tsx`: 파일 업로드 UI
- `URLAnalyzer.tsx`: URL 분석 UI
- `AnalysisResult.tsx`: 분석 결과 표시
- `AIInsight.tsx`: AI 분석 표시
- `CreditCharge.tsx`: 티켓 충전

**기능**:
- 사용자 인터페이스 제공
- 파일/URL 업로드
- 분석 결과 시각화
- 인증 토큰 관리 (localStorage)

---

### 2. 애플리케이션 계층 (Application Layer)

**구성 요소**: Backend (FastAPI)

```
┌─────────────────────────────────────────┐
│         Backend (FastAPI)               │
├─────────────────────────────────────────┤
│  • API Endpoints                        │
│  • Business Logic                       │
│  • Authentication (JWT)                 │
│  • Rate Limiting                        │
└─────────────────────────────────────────┘
```

**주요 모듈**:
- `main.py`: API 라우트 및 엔드포인트
- `auth.py`: JWT 인증
- `models.py`: 데이터베이스 모델
- `analyzer.py`: 파일 분석 엔진
- `external_apis.py`: 외부 API 통합

**API 엔드포인트**:
- `POST /auth/login`: 로그인
- `GET /auth/me`: 사용자 정보
- `POST /files/upload`: 파일 업로드 및 분석
- `POST /url/analyze`: URL 분석
- `POST /analysis/ai`: AI 심층 분석
- `GET /analysis/{scan_id}`: 분석 결과 조회
- `GET /analysis/history`: 분석 이력
- `DELETE /analysis/{scan_id}`: 분석 삭제
- `POST /credits/charge`: 티켓 충전
- `GET /credits/history`: 충전 이력

---

### 3. 분석 엔진 계층 (Analysis Engine Layer)

**구성 요소**: 분석 모듈들

```
┌─────────────────────────────────────────┐
│      Analysis Engine (analyzer.py)      │
├─────────────────────────────────────────┤
│  ┌───────────────────────────────────┐  │
│  │ 1. ClamAV Scanner                 │  │
│  │    → Socket (clamav:3310)        │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │ 2. YARA Rules Engine              │  │
│  │    → Pattern Matching             │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │ 3. Entropy Calculator             │  │
│  │    → Packing Detection             │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │ 4. File Type Verifier             │  │
│  │    → python-magic                 │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │ 5. Binary Analyzer                │  │
│  │    → Shellcode, Strings           │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │ 6. PE Analyzer (pefile)           │  │
│  │    → Header, Sections, Imports    │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │ 7. PE Enhanced Analyzer (LIEF)    │  │
│  │    → Section Entropy, APIs        │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │ 8. Office Analyzer (oletools)    │  │
│  │    → VBA Macros, Auto-exec        │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │ 9. PDF Analyzer (PyPDF2)          │  │
│  │    → JavaScript, Actions          │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │ 10. ZIP Analyzer                  │  │
│  │     → Nested Archives, Encryption │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │ 11. String Extractor              │  │
│  │     → URLs, IPs, Emails           │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │ 12. Email Analyzer                 │  │
│  │     → Spearphishing Indicators     │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │ 13. External API Analyzer         │  │
│  │     → VirusTotal, MalwareBazaar  │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │ 14. Risk Score Calculator         │  │
│  │     → 0-100 Score                 │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

**분석 프로세스**:
1. 파일 검증
2. 병렬/순차 분석 실행 (15개 분석 툴)
3. 위험도 점수 계산
4. 결과 저장

---

### 4. 데이터 계층 (Data Layer)

**구성 요소**: MySQL Database

```
┌─────────────────────────────────────────┐
│         MySQL Database                  │
├─────────────────────────────────────────┤
│  ┌───────────────────────────────────┐  │
│  │ users                             │  │
│  │  • id (PK)                        │  │
│  │  • username                       │  │
│  │  • password_hash                  │  │
│  │  • role                           │  │
│  │  • credits                        │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │ analyses                          │  │
│  │  • scan_id (PK)                   │  │
│  │  • user_id (FK)                   │  │
│  │  • filename                       │  │
│  │  • analysis_data (JSON)           │  │
│  │  • risk_score                     │  │
│  │  • risk_level                     │  │
│  │  • ai_analysis (TEXT)             │  │
│  │  • uploaded_at                    │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │ credit_purchases                  │  │
│  │  • id (PK)                        │  │
│  │  • user_id (FK)                   │  │
│  │  • amount                         │  │
│  │  • balance_after                  │  │
│  │  • purchased_at                   │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

**데이터 모델**:
- **User**: 사용자 정보 및 크레딧
- **Analysis**: 분석 결과 및 이력
- **CreditPurchase**: 티켓 구매 이력

---

### 5. 외부 서비스 계층 (External Services Layer)

**구성 요소**: 외부 API 및 서비스

```
┌─────────────────────────────────────────┐
│      External Services                  │
├─────────────────────────────────────────┤
│  ┌───────────────────────────────────┐  │
│  │ ClamAV Container                 │  │
│  │  • Socket: clamav:3310           │  │
│  │  • Virus Signature Database      │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │ VirusTotal API                    │  │
│  │  • Hash-based Lookup              │  │
│  │  • Multiple AV Engines            │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │ MalwareBazaar API                 │  │
│  │  • Malware Sample Database        │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │ URLScan.io API                    │  │
│  │  • URL Scanning                   │  │
│  │  • Screenshot                     │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │ IP-API                            │  │
│  │  • Geolocation                    │  │
│  │  • ISP Information                │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │ Google Gemini API                 │  │
│  │  • AI Analysis                    │  │
│  │  • Natural Language Generation    │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

---

## 🔄 데이터 흐름 (Data Flow)

### 파일 분석 흐름

```
1. 사용자 파일 업로드
   │
   ▼
2. Frontend → Backend (POST /files/upload)
   │
   ▼
3. Backend: 파일 검증
   │
   ▼
4. Backend: 파일 저장 (/tmp/securelens_uploads)
   │
   ▼
5. Backend: 분석 엔진 실행
   │
   ├─→ ClamAV (Socket: clamav:3310)
   ├─→ YARA (Local Rules)
   ├─→ Entropy Calculator
   ├─→ File Type Verifier
   ├─→ Binary Analyzer
   ├─→ PE Analyzer (pefile)
   ├─→ PE Enhanced Analyzer (LIEF)
   ├─→ Office Analyzer (oletools)
   ├─→ PDF Analyzer (PyPDF2)
   ├─→ ZIP Analyzer
   ├─→ String Extractor
   ├─→ Email Analyzer
   └─→ External APIs
       ├─→ VirusTotal (HTTP)
       ├─→ MalwareBazaar (HTTP)
       ├─→ URLScan.io (HTTP)
       └─→ IP-API (HTTP)
   │
   ▼
6. Backend: 위험도 점수 계산
   │
   ▼
7. Backend: MySQL에 결과 저장
   │
   ▼
8. Backend → Frontend (Response)
   │
   ▼
9. Frontend: 결과 페이지로 리다이렉트
```

### AI 심층 분석 흐름

```
1. 사용자 AI 분석 요청
   │
   ▼
2. Frontend → Backend (POST /analysis/ai)
   │
   ▼
3. Backend: 티켓 확인
   │
   ▼
4. Backend: MySQL에서 분석 결과 조회
   │
   ▼
5. Backend: 프롬프트 구성
   │
   ├─→ 일반 분석 결과
   ├─→ 강화 분석 결과
   └─→ 이메일 정보 (선택사항)
   │
   ▼
6. Backend → Google Gemini API
   │
   ▼
7. Gemini: AI 분석 생성
   │
   ▼
8. Backend: MySQL에 AI 분석 저장
   │
   ▼
9. Backend: 티켓 차감
   │
   ▼
10. Backend → Frontend (Response)
    │
    ▼
11. Frontend: AI 분석 결과 표시
```

---

## 🐳 컨테이너 아키텍처

### Docker Compose 구조

```
┌─────────────────────────────────────────┐
│      Docker Network                     │
│      (securelens-network)               │
├─────────────────────────────────────────┤
│                                         │
│  ┌─────────────────────────────────┐  │
│  │ nginx                            │  │
│  │  • Port: 80                      │  │
│  │  • Reverse Proxy                 │  │
│  └─────────────────────────────────┘  │
│                                         │
│  ┌─────────────────────────────────┐  │
│  │ frontend                         │  │
│  │  • Port: 3000 (internal)        │  │
│  │  • Next.js                      │  │
│  │  • Build: Dockerfile.prod        │  │
│  └─────────────────────────────────┘  │
│                                         │
│  ┌─────────────────────────────────┐  │
│  │ backend                          │  │
│  │  • Port: 8000 (internal)        │  │
│  │  • FastAPI                      │  │
│  │  • Python 3.10                  │  │
│  └─────────────────────────────────┘  │
│                                         │
│  ┌─────────────────────────────────┐  │
│  │ mysql                           │  │
│  │  • Port: 3306                  │  │
│  │  • Volume: mysql-data          │  │
│  └─────────────────────────────────┘  │
│                                         │
│  ┌─────────────────────────────────┐  │
│  │ clamav                           │  │
│  │  • Port: 3310 (internal)        │  │
│  │  • Volume: clamav-data         │  │
│  └─────────────────────────────────┘  │
│                                         │
└─────────────────────────────────────────┘
```

### 컨테이너 간 통신

- **Frontend ↔ Backend**: HTTP (내부 네트워크)
- **Backend ↔ MySQL**: MySQL Protocol (3306)
- **Backend ↔ ClamAV**: Socket (3310)
- **Backend ↔ External APIs**: HTTPS

---

## 🔐 보안 아키텍처

### 인증 및 인가

```
┌─────────────────────────────────────────┐
│         Authentication Flow              │
├─────────────────────────────────────────┤
│                                         │
│  1. 사용자 로그인                        │
│     POST /auth/login                    │
│                                         │
│  2. Backend: 비밀번호 검증               │
│     • bcrypt 해시 비교                  │
│                                         │
│  3. Backend: JWT 토큰 생성              │
│     • Payload: {sub: username}          │
│     • Expiry: 24시간                    │
│                                         │
│  4. Frontend: 토큰 저장                 │
│     • localStorage                      │
│                                         │
│  5. 이후 요청: Bearer Token              │
│     Authorization: Bearer <token>        │
│                                         │
│  6. Backend: 토큰 검증                  │
│     • JWT 서명 확인                     │
│     • 만료 시간 확인                    │
│                                         │
└─────────────────────────────────────────┘
```

### 파일 보안

- **격리 저장**: `/tmp/securelens_uploads` (컨테이너 내부)
- **자동 삭제**: 1시간 후 스레드로 삭제
- **크기 제한**: 50MB
- **확장자 화이트리스트**: 허용된 확장자만
- **경로 탐색 방지**: `..`, `/`, `\` 제거

### API 보안

- **Rate Limiting**: IP당 시간당 10개
- **CORS**: 허용된 Origin만
- **입력 검증**: Pydantic 모델
- **타임아웃**: 모든 스캔 30초

---

## 📊 확장성 고려사항

### 수평 확장 (Horizontal Scaling)

```
                    ┌─────────┐
                    │  Nginx  │
                    │ (Load   │
                    │ Balancer)│
                    └────┬────┘
                         │
        ┌────────────────┼────────────────┐
        │                │                │
        ▼                ▼                ▼
   ┌─────────┐    ┌─────────┐    ┌─────────┐
   │Backend 1│    │Backend 2│    │Backend 3│
   └────┬────┘    └────┬────┘    └────┬────┘
        │              │              │
        └──────────────┼──────────────┘
                       │
                  ┌────▼────┐
                  │  MySQL  │
                  │(Master) │
                  └─────────┘
```

**확장 가능한 컴포넌트**:
- Frontend: Stateless, 무제한 확장 가능
- Backend: Stateless, 로드 밸런서로 확장 가능
- MySQL: Master-Slave 복제

### 수직 확장 (Vertical Scaling)

- **Backend**: CPU/메모리 증가 (분석 작업)
- **MySQL**: 메모리 증가 (쿼리 성능)
- **ClamAV**: 메모리 증가 (시그니처 DB)

---

## 🔄 배포 아키텍처

### CI/CD 파이프라인

```
┌─────────────────────────────────────────┐
│      GitHub Repository                  │
│      (Code Push)                        │
└───────────────┬─────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────┐
│      GitHub Actions                     │
│      (.github/workflows/deploy.yml)     │
├─────────────────────────────────────────┤
│  1. 코드 체크아웃                       │
│  2. Docker 이미지 빌드                   │
│  3. 서버에 SSH 연결                     │
│  4. 이미지 업데이트                     │
│  5. docker-compose.app.yml 실행         │
│  6. 컨테이너 재시작                     │
└───────────────┬─────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────┐
│      Production Server                  │
│      (Docker Containers)                │
└─────────────────────────────────────────┘
```

---

## 📈 성능 최적화

### 캐싱 전략

- **Frontend**: Next.js 자동 캐싱
- **Backend**: 분석 결과는 DB에 저장 (재조회 시 빠름)
- **ClamAV**: 시그니처 DB는 메모리에 로드

### 비동기 처리

- **파일 삭제**: 백그라운드 스레드
- **외부 API**: 비동기 호출 (타임아웃 설정)
- **분석 작업**: 순차 실행 (의존성 고려)

### 리소스 관리

- **메모리**: 파일 크기 제한 (50MB)
- **CPU**: 타임아웃으로 무한 루프 방지
- **디스크**: 자동 파일 삭제

---

## 🛠️ 기술 스택 요약

| 계층 | 기술 | 용도 |
|------|------|------|
| **Frontend** | Next.js, React, TypeScript, Tailwind CSS | 사용자 인터페이스 |
| **Backend** | FastAPI, Python 3.10 | API 서버 및 비즈니스 로직 |
| **Database** | MySQL 8.0 | 데이터 영속성 |
| **Reverse Proxy** | Nginx | 라우팅 및 로드 밸런싱 |
| **Antivirus** | ClamAV | 바이러스 탐지 |
| **Analysis Tools** | YARA, pefile, oletools, PyPDF2, python-magic, LIEF | 파일 분석 |
| **AI** | Google Gemini 2.5 Flash | 심층 분석 |
| **Container** | Docker, Docker Compose | 컨테이너화 |
| **CI/CD** | GitHub Actions | 자동 배포 |

---

## 🎯 아키텍처 설계 원칙

1. **마이크로서비스**: 각 서비스는 독립적으로 동작
2. **컨테이너화**: Docker로 일관된 환경
3. **Stateless**: 상태는 DB에 저장
4. **RESTful API**: 표준 HTTP 메서드 사용
5. **보안 우선**: 인증, 검증, 격리
6. **확장 가능**: 수평/수직 확장 지원
7. **모니터링 가능**: 로그 및 에러 추적

---

**이 아키텍처는 보안, 확장성, 유지보수성을 고려하여 설계되었습니다.**

